scalar RegEx

directive @constrainedString(
    minLength: Int,
    maxLength: Int,
    startsWith: String,
    endsWith: String,
    includes: String,
    excludes: String,
    pattern: RegEx
) on SCALAR