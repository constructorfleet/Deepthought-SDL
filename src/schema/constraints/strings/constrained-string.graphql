scalar RegularExpression

directive @constrainedString(
    minLength: Int,
    maxLength: Int,
    startsWith: String,
    endsWith: String,
    includes: String,
    excludes: String,
    pattern: RegularExpression
) on SCALAR